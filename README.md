## Overview
A simple Python automation tool that organizes files based on their extensions. It scans a folder for supported file types such as CSV and JSON, then automatically uses the os and shutil function modules to move them into their respective directories to keep the workspace clean and structured.
